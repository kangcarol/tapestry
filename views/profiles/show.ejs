<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<main>
  <h1> <%= profile.name %></h1>

  <% if (profile.avatar) { %>
    <div>
      <img id="avatar"
        src="<%= profile.avatar %>"
        alt="<%= profile.name %> avatar"
      />
    </div>
  <% } %>
</main>

<%- include('../partials/footer') %>
