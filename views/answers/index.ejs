<%- include('../partials/html-head') %>
<%- include('../partials/nav') %>

<section>
  <h1>ANSWERS FEED</h1>

<!-- attach the question -->
  <ul>
    <% answers.forEach(answer => { %>
      <li>
        <!-- add author's avatar and name -->
        <%= answer.author %></a>
        <%= answer.text %></a>
    <% }) %>
  </ul>
</section>

<%- include('../partials/footer') %>
